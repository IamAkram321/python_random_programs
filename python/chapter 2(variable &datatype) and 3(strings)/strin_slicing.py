#Concatenation of two strings
# greeting = "Good Morning, "
# name = "Akram"
# print(greeting + name)

#string slicing
name = "Akram"
# print(name[0])
# print(name[0:3]) 
print(name[0::3]) #If we don't keep (i.e 0::3)value then it automatically takes the last vale